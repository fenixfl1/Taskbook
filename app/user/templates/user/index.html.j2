{% extends 'base.html' %} {% block content %}

<div id="content-wrapper" class="d-flex flex-column">

    <!-- Main Content -->
    <div id="content">

        <!-- Begin Page Content -->
        <div class="container-fluid">

            <div class="row">
                {# the next task to perform #}
                <div class="col-xl-3 col-md-6 mb-4">
                    <div class="card shadow h-100 py-2">
                        <div class="card-body">
                            <div class="row no-gutters align-items-center">
                                {% if task_user != [] %}
                                    <div class="col mr-2">
                                        <div class="text-xs text-center font-weight-bold text-segundary text-uppercase mb-1">
                                            <p> <i class="fas fa-circle text-info"></i> próxima Tarea.</p>
                                        </div>

                                        <hr class="sidebar-divider">

                                         <span class="mr-2">
                                            <i class="fas fa-tasks text-info circle"></i>
                                        </span>
                                        
                                        <div class="mb-0 font-weight-bold text-gray-400 float-right">
                                            
                                            <a href=" {{url_for('users.details_task', id=task_user[0].id)}} " title="Ver detalles">
                                                {{ task_user[0].name }}
                                            </a>
                                        </div>
                                    </div>
                                {% endif %}
                            </div>
                        </div>
                    </div>
                </div>

                {# the next activity to plan estudios #}
                <div class="col-xl-3 col-md-6 mb-4">
                    <div class="card shadow h-100 py-2">
                        <div class="card-body">
                            <div class="row no-gutters align-items-center">
                                {% if task_user != [] %}
                                    <div class="col mr-2">
                                        <div class="text-xs text-center font-weight-bold text-segundary text-uppercase mb-1">
                                            <p><i class="fas fa-circle text-primary"></i> próximo en Plan de Estudio.</p>
                                        </div>

                                        <hr class="sidebar-divider">

                                         <span class="mr-2">
                                            <i class="fas fa-fw fa-book-reader text-primary"></i>
                                        </span>
                                        
                                        <div class="mb-0 font-weight-bold text-gray-400 float-right">
                                            
                                            <a href=" {{url_for('users.details_task', id=task_user[0].id)}} " title="Ver detalles">
                                                Curso de React JS
                                            </a>
                                        </div>
                                    </div>
                                {% endif %}
                            </div>
                        </div>
                    </div>
                </div>

                {# the next event #}
                <div class="col-xl-3 col-md-6 mb-4">
                    <div class="card shadow h-100 py-2">
                        <div class="card-body">
                            <div class="row no-gutters align-items-center">
                                {% if task_user != [] %}
                                    <div class="col mr-2">
                                        <div class="text-xs text-center font-weight-bold text-segundary text-uppercase mb-1">
                                            <p><i class="fas fa-circle text-success"></i> próximo Evento.</p>
                                        </div>

                                        <hr class="sidebar-divider">

                                         <span class="mr-2">
                                            <i class="fas fa-calendar text-success"></i>
                                        </span>
                                        
                                        <div class="mb-0 font-weight-bold text-gray-400 float-right">
                                            
                                            <a href=" {{url_for('users.details_task', id=task_user[0].id)}} " title="Ver detalles">
                                                Seminario I
                                            </a>
                                        </div>
                                    </div>
                                {% endif %}
                            </div>
                        </div>
                    </div>
                </div>

                {# the next class #}
                <div class="col-xl-3 col-md-6 mb-4">
                    <div class="card shadow h-100 py-2">
                        <div class="card-body">
                            <div class="row no-gutters align-items-center">
                                {% if task_user != [] %}
                                    <div class="col mr-2">
                                        <div class="text-xs text-center font-weight-bold text-segundary text-uppercase mb-1">
                                            <p><i class="fas fa-circle text-warning"></i> próxima clase.</p>
                                        </div>

                                        <hr class="sidebar-divider">

                                         <span class="mr-2">
                                            <i class="fas fa-clipboard-list text-warning"></i>
                                        </span>
                                        
                                        <div class="mb-0 font-weight-bold text-gray-400 float-right">
                                            
                                            <a href=" {{url_for('users.details_task', id=task_user[0].id)}} " title="Ver detalles">
                                                Diseño de Sistema
                                            </a>
                                        </div>
                                    </div>
                                {% endif %}
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Content Row -->

            <div class="row">

                <!-- Area horario de clase -->
                <div class="col-xl-8 col-lg-7">

                    <div class="card shadow mb-4">
                        <!-- Card Header - title -->
                        {# <div class="card-header py-3 d-flex flex-row align-items-center justify-content-between">
                            <h6 class="m-0 font-weight-bold text-primary">Horario de clase</h6>
                        </div> #}

                        <!-- Card Body -->
                        <div class="card-body">
                            <div class="chart-area">

                            </div>
                        </div>
                    </div>
                </div>

                <!-- Pie Chart -->
                <div class="col-xl-4 col-lg-5">
                    <div class="card shadow mb-4">
                        <!-- Card Header - Dropdown -->
                        {# <div class="card-header py-3 d-flex flex-row align-items-center
							justify-content-between">
                            <h6 class="m-0 font-weight-bold text-primary">Notas</h6>
                            <div class="dropdown no-arrow">
                                <a class="dropdown-toggle" href="#" role="button" id="dropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                    <i class="fas fa-ellipsis-v fa-sm fa-fw text-gray-400"></i>
                                </a>
                                <div class="dropdown-menu dropdown-menu-right shadow animated--fade-in" aria-labelledby="dropdownMenuLink">
                                    <div class="dropdown-header">Acciones:</div>
                                    <a class="dropdown-item" href="#">
                                        <i class="fas fa-trash-alt fa-sm fa-fw mr-2 text-gray-400"></i> Eliminar
                                    </a>
                                    <a class="dropdown-item" href="#">
                                        <i class="fas fa-edit fa-sm fa-fw mr-2 text-gray-400"></i> Editar
                                    </a>
                                    <div class="dropdown-divider"></div>
                                    <a class="dropdown-item" href="#">
                                        <i class="fas fa-file fa-sm fa-fw mr-2 text-gray-400"></i> Nueva Nota
                                    </a>
                                </div>
                            </div>
                        </div> #}
                        <!-- Card Body -->
                        <div class="card-body">
                            <div class="chart-pie pt-4 pb-2">
                                <canvas id="myPieChart"></canvas>
                            </div>

                            <hr class="sidebar-divider">

                            {# <div class="mt-4 text-center small">
                                <span class="mr-2">
									<i class="fas fa-circle text-primary"></i> Direct
								</span>
                                <span class="mr-2">
									<i class="fas fa-circle text-success"></i> Social
								</span>
                                <span class="mr-2">
									<i class="fas fa-circle text-info"></i> Referral
								</span>
                            </div> #}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}