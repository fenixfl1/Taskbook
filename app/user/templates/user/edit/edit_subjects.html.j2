{% extends "base.html" %} {% from "macro.html" import Form_off_label as form %}
{% block content %}
    <div id="content-wrapper" class="d-flex flex-column">
        <div id="content">
            <div class="container-fluid">
                <ol class="breadcrumb" aria-label="breadcrumb">
                    <li class="breadcrumb-item active">
                        <a href="/subjects/"> Materias({{ num_subjects }})</a>
                    </li>
                                                
                    <li class="breadcrumb-item">
                        Edición
                    </li>
                </ol>

                <div class="row m-auto">
                    <form class="form-inline" action=" {{url_for('auth.update_subjects', id=edit_data.id)}} " method="POST">
                        {{ subject_form.hidden_tag() }}
                        <div class="form-group my-4">
                            {{ form(subject_form.name, class="form-control form-control", value=edit_data.name) }}
                        </div>
                                                    
                        <div class="text-center form-group">
                            {{ form(subject_form.submit, class="btn btn-primary", value="Editar") }}
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>

{% endblock  %}