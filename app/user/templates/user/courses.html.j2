{% extends 'base.html' %} {% block content %}
	{% from 'macro.html' import Form_off_label, navbar %}
	
	{# Alerts#}
	{% include 'messages.html' %}
	{# modal to ass new task #}
	{% include "forms/task_form.html.j2" %}
	{# modal to edit subjects #}
	{% include "forms/edit/subjects.html.j2" %}
	{# modal to assing teachers #}
	{% include "forms/edit/assing_teachers.html.j2" %}
	<div class="container-fluid">
		
		<div class="row justify-content-md-between mt-3" style="height: 85vh">
			{# sidebar left #}
			<div class="col-md-2 ml-1">
				<button type="button" title="{{title}}"
				class="btn btn-secondary d-none d-sm-inline-block"
				data-toggle="modal" data-target="#staticBackdrop">
				<i class="fas fa-plus"></i>
				Agregar nuevo
				</button>
			</div>
			{# main content #}
			<div class="col-md-7 mx-2 card-container">
				<div class="row row-card justify-content-md-start">
					{% if course.task %}
						{% for i in range(tasks | tasks(0) ) %}
							<div class="card mx-2 card-row mb-5">
								<figure>
									{{ assignments[i].name | generate_avatar(size=300,
									class='card-img-top') }}
								</figure>
								<div class="card-body text-center">
									<h5 class="card-title">Card title</h5>
									<p class="card-text">Some quick example text to build on the </p>
									<a href="#" class="btn btn-primary mx-auto">Go somewhere</a>
								</div>
							</div>
						{% endfor %}
					{% else %}
						<img src="{{ url_for('static',filename='') }}" alt="">
					{% endif %}
				</div>
			</div>
			{# sidebar right #}
			<div class="col-10 col-md-2 mr-1 d-none d-sm-inline-block">
				{# <div class="col-10 col-md-3 mx-auto"> #}
					<div class="list-group bg-white col-md-12" id="list-ta" role="tablist">
						<a class="list-group-item list-group-item-action active"
							id="list-home-list" data-toggle="list"
							href="#list-home" role="tab"
							aria-controls="home">
							Tareas pendientes
						</a>
						<a class="list-group-item list-group-item-action"
							id="list-profile-list" data-toggle="list"
							href="#list-profile" role="tab"
							aria-controls="profile">
							Tarea completadas
						</a>
						<a class="list-group-item list-group-item-action"
							id="list-messages-list" data-toggle="list"
							href="#list-messages" role="tab"
							aria-controls="messages">
							Messages
						</a>
						<a class="list-group-item list-group-item-action"
							id="list-settings-list" data-toggle="list"
							href="#list-settings" role="tab"
							aria-controls="settings">
							Settings
						</a>
					</div>
				{# </div> #}
			</div>
		</div>
	</div>
{% endblock %}
{% block footer %}
	<li class="nav-item">
		<a href="" title="" class="nav-link">
			<img src="{{ url_for('static',filename='icons-svg/icons/') }}" alt="">
			
		</a>
	</li>
	<li class="nav-item">
		<a class="nav-link" href="/" title="Home">
			<img src=" {{ url_for('static',filename='icons-svg/icons/house.svg') }} " alt="Search" class="nav-icon">
			
		</a>
	</li>

	<li class="nav-item dropdown no-arrow mx-1 d-none d-sm-inline-block">
		<a class="nav-link dropdown-toggle" href="#" id="alertsDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
			<img src=" {{ url_for('static',filename='icons-svg/icons/columns-gap.svg') }} " alt="Courses" class="nav-icon">
			<!-- Courses -->
			<span class="badge badge-danger badge-counter"></span>
		</a>
		<div class="dropdown-list dropdown-menu dropdown-menu-right shadow animated--grow-in" aria-labelledby="alertsDropdown">
			<h6 class="dropdown-header"> Mis cursos </h6>
			{% if courses | courses(0) > 0 %}
				{% set pagination = (1 | paginate_courses(0) ) %}
				{% for course in pagination.items %}
					<a class="dropdown-item d-flex align-items-center"
						href="{{ url_for('users.courses',id=course.id) }}">
						<div class="mr-3">
							<div class="icon-circle bg-primary">
								{{ course.name | generate_avatar(size=300,
								class='card-img-top rounded-circle') }}
							</div>
						</div>
						<div>
							<div class="small text-gray-500"> {{ course.teacher }} </div>
							<span class="font-weight-bold">
								{{ course.name }}
							</span>
						</div>
					</a>
				{% endfor %}
				<a class="dropdown-item text-center small text-gray-500" href="#">
					Ver todos los cursos
				</a>
			{% else %}
				<a href="/courses" title="">Ningun curso agregardo</a>
			{% endif %}
		</div>
	</li>
{% endblock %}